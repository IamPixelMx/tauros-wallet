import styled from '@emotion/styled';
import { css } from '@emotion/core';

export const CreditCardStyled = styled.div`
  width: 335px;
  height: 207px;
  text-align: center;
  vertical-align: middle;
  border-radius: 18px;
  box-shadow: 0 10px 38px -10px rgba(134, 150, 168, 0.5),
    0 10px 10px -10px rgba(134, 150, 168, 0.25);
  border: solid 0.5px rgba(0, 0, 0, 0.66);
  background-image: linear-gradient(to bottom, var(--mango), var(--light-gold) 50%);
  margin-top: var(--spacing-xl);
  margin-bottom: var(--spacing-l);
`;
